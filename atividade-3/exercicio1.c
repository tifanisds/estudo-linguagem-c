#include <stdio.h>

int main() {
    int numero = 11;

    if (numero % 2 == 0) {
        printf("O número %d é par!\n", numero);
    } else {
        printf("O número %d é impar!\n", numero);
    }

    return 0;
}
